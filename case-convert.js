const savedName = 'blackPink';
const linkName = 'BlackPink';
const input = linkName.toLocaleUpperCase();
console.log(input);
if(savedName.toLocaleLowerCase() == linkName.toLocaleLowerCase()){
console.log('both are Same');
}